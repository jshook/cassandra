#!/bin/bash

scriptdir=$(dirname "$0")
printf "scriptdir: %s\n" $scriptdir
BYTEMAN="build/lib/jars/byteman-3.0.3.jar"
IOLOG="byteman/iolog.btm"

JVM_OPTS="-javaagent:${BYTEMAN}=script:${IOLOG},boot:${BYTEMAN} -Dorg.jboss.byteman.transform.all" \
$scriptdir/cassandra $*
